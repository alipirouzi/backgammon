name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [master]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  engine:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: engine
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.98.0
          components: rustfmt, clippy
          targets: wasm32-unknown-unknown
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: engine
      - run: cargo fmt --check
      - run: cargo clippy --all-targets -- -D warnings
      - run: cargo test
      - name: Perf budget and full decision-vector drift (release)
        run: |
          set -o pipefail
          cargo test --release -p bg-bot --test perf --test vectors -- --include-ignored --show-output 2>&1 | tee release-tests.log
          grep -q '^test perf_mean_club_decision_from_middlegame_is_under_budget \.\.\. ok$' release-tests.log
          grep -q '^test committed_decisions_json_equals_regenerated_output \.\.\. ok$' release-tests.log
      - run: cargo build --target wasm32-unknown-unknown -p bg-core -p bg-bot

  web:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v5
        with:
          node-version-file: .nvmrc
          cache: pnpm
      - run: pnpm install --frozen-lockfile
        working-directory: .
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm test -- --coverage
      - run: pnpm build
      - run: pnpm exec playwright install --with-deps chromium
      - run: pnpm test:e2e
        id: e2e
      - uses: actions/upload-artifact@v4
        # Only when the e2e step itself failed; the HTML report is configured in web/playwright.config.ts.
        if: ${{ failure() && steps.e2e.outcome == 'failure' }}
        with:
          name: playwright-report
          path: web/playwright-report/
